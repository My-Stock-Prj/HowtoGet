# 핵심 라이브러리
pandas
numpy

# Parquet 파일 처리를 위한 엔진 (엔진 하나만 확실히 지정)
pyarrow
fastparquet

# 구글 시트 연동 (에러 해결을 위해 버전을 명시)
gspread
google-auth
gspread-dataframe>=3.3.0

# 엑셀 파일 저장용 (df.to_excel 사용 시 필수)
openpyxl

# API 통신
requests
